# SQLite (default - no setup required)
DATABASE_URL=sqlite:///./usage_billing.db

# PostgreSQL (if you have it installed)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/usage_billing_db
